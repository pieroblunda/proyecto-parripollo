# Falla en la cadena de produccion

`Fragmentación por roles y pérdida de visión sistémica del problema real`

* Falla la cadena de produccion. Hay bugs, porque el documento esta mal ecrito. El documento esta mal escrito porque el cliene no sabe. El cliente no sabe porque........ y asi. No esta claro el limite del campo de accion.
